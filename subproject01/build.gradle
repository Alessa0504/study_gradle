plugins {
    id 'java'
}

group 'org.example'
version '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

dependencies {
    testCompile group: 'junit', name: 'junit', version: '4.12'
}

task 'A' {
    group('group one')   //给任务指定组，否则会被分在other中
    doLast {
        println("task A")
    }
}
